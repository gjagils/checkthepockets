services:
  app:
    image: ghcr.io/gjagils/checkthepockets:latest
    container_name: checkthepockets
    restart: unless-stopped
    env_file:
      - path: ./stack.env
        required: false
    ports:
      - "${APP_PORT:-8368}:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - SECRET_KEY=${SECRET_KEY:-verander-dit-in-een-lang-willekeurig-wachtwoord}
